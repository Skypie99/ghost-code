# DECISIONS.md — Ghost Code UPLIFT train ledger (cross-window state)

> **Scope:** train-scoped decisions for the fable-audit build train ONLY. This is deliberately
> DISTINCT from repo-root `DECISIONS_LOG.md` (structural project decisions) — **do not merge the
> two.** Every phase window reads this file FIRST and records picks here AS THEY HAPPEN: a pick made
> in phase 2's window must reach phase 4's fresh window as disk state, not chat history.
>
> **Design authority:** `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/2026-07-10_GhostCode_Design_Review.md`

## A · Standing decisions (already made by the report — execute, never re-open)

| # | Decision | Source |
|---|---|---|
| SD-1 | **S3 button weighting is DECIDED:** DRILL MISSED (n) becomes the filled/bright PRIMARY (the treatment PLAY AGAIN has today); PLAY AGAIN drops to a secondary ghost/outline button, same size class, hotkeys/function unchanged. No gate. | Report S3 · What |
| SD-2 | **Q7 is a glyph-swap + aria-semantics change ONLY.** Its verification demands a glyph-only diff (no label text added/removed/reworded). The "50/50 appears twice" observation is fenced out-of-scope by the report itself. | Report Q7 |
| SD-3 | **S1↔M2 paint boundary:** S1's held win-state terminates in the SAME paint as M2's judgment-class clear. M2 lands in P1; P1 records the clear's **code anchor** at SD-9. S1 must never ship without that clear in place (else CRITICAL L4-F1 returns). | Report S1 · Effort |
| SD-4 | **S1 changes WHAT fills the 750ms/1200ms windows, never their length** — auto-advance pacing is fenced. | Report S1 · What |
| SD-5 | **M1 stays within the directional diamond.** The `.n/.e/.s/.w` NESW mapping is load-bearing for swipe AND arrow-keys; any grid/list reflow is the fenced Fork E (Sky + Dani input-model decision, not this train). | Report M1 + Fork E |
| SD-6 | **M4 is scoped to the title→board swap only** — `.screen` + its transition are shared by title, game-over, and pause; pause + game-over fades must remain byte-identical. | Master LAW 3 |
| SD-7 | **Q5 is preload-only** (self-hosting vs accepting the fetch = fenced Sky/Rory infra call). **M5's favicon is recolor-only, never redraw** (favicon↔mascot geometry chain is PROTECT). | Report Q5 / M5 |
| SD-8 | **Model directive (Sky, recorded 2026-07-16):** execution on **Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort**; Sky fires each phase window herself. Authored by Fable 5 max. | Sky's instruction |
| SD-9 | **M2 clear anchor = `renderCard()`'s token-reset loop** — `DOM.tokens.forEach(d => d.classList.remove('correct','wrong','captured','eliminated'))`, marked in-code by the `[M2 · L4-F1 · SD-9]` comment. This clear runs in the SAME synchronous paint as the prompt swap (`DOM.promptText.textContent`) and the token-text swap. **Verified atomic** at per-frame (rAF) resolution on every advance path (arcade correct/wrong · learn correct · learn reveal→next), both RM routes — **L4-F1 does NOT reproduce under Chromium** (CDP media + in-app `body.reduce-motion`); real OS-level RM = NEEDS-SKY-DEVICE. **S1 (P3) MUST tear down its held win-state in THIS clear.** Anchor is a function/selector, never a SHA. | P1 wrote 2026-07-17 (commit 6d150c4) |
| SD-10 | **Fences held train-wide:** presentation-only; `textContent`-only for card-derived text; `[PERSIST-ADDITIVE]` gc.v1; `[LIFELINE-SESSION-ONLY]`; no invented copy/claims/numbers; accessibility outranks aesthetics; PROTECT list per the report §PROTECT. | Report |
| SD-11 | **Scheduling mode:** static site, locally openable → per-phase device checklists + ONE consolidated close-out device session (P6). Chromium ≠ Safari; Sky's device pass is the only Safari truth. | Master Step 0 |

## B · Open forks & gates (status: OPEN until a Sky pick is recorded in §D)

| Gate | Phase | Options | Status |
|---|---|---|---|
| Fork E (FYI) | P1 pre-flight | Proceed keep-diamond (default, per report) · redirect to fenced reflow (halts M1) | **OPEN — default proceeds** |
| M1 de-collision form | P1 | 3 rendered @375, longest card forced: A slim-phantom-only INSUFFICIENT; B narrow tokens +22px/3-line; C balanced +10px/2-line. | **PICKED: C — Balanced (Sky, 2026-07-17)** → build in Item 5 |
| OG-card composition | P2 | 2–3 candidates in shipped identity | **PICKED: A — editorial (Sky, 2026-07-17 in-window)** → exported to og-image.png + truthful mascot-free alt (`c4b06dc`) |
| Fork C (controls line) | P2 | keep (Q1-fixed) · remove (rely on `?` modal) | **PICKED: KEEP & fix (Sky, 2026-07-17 P2 plan gate)** → Q1 builds; keep-vs-remove mocks still rendered for a later final call |
| Fork D (name curriculum pre-play) | P2 | yes (→ PARKING-LOT item, un-slated) · no | **PICKED: NO / keep-generic (Sky, 2026-07-17 P2 plan gate)** → no parking-lot entry |
| Fork A (win-beat form) | P3 | held green+✓ only · lingering "+N" · full-command burn-in · all three | **PICKED: ALL THREE (Sky, 2026-07-17 P3 plan gate)** → S1 built (S1a `0cf8bca` + S1b `161ae66`) |
| Q7 glyph form | P3 | heart/pulse (SPIRITS) + slashed-circle/fork (50/50) · filled-hollow blocks · minimal star-swap only | **PICKED: HEART + SLASHED-CIRCLE (Sky, 2026-07-17 P3 plan gate)** → Q7 built (`be022b6`) |
| Fork F (progress credit) | P4 | presentation row (M6 builds) · fenced mastery-rule change (M6 → FORKED, dropped) | **PICKED: presentation row (Sky, 2026-07-18 P4 plan gate)** → M6 builds the "+N this run" credit + Learn-bar aria un-hide; c≥3 rule untouched |
| Fork B (title-Phantom form) | P5 | idle-float cursor near CTA · static ghost-glyph · one-shot materialize (all RM-gated) | **PICKED: C — one-shot materialize** (Sky, 2026-07-29, recorded coming into the P5 window) → built directly, no throwaway candidate mocks needed |
| Q10 wording | P2 | slate example ships ("Welcome back — pick up where you left off"); Sky may re-word any time | **SKY-WORDING — non-blocking** |

## C · PENDING-SKY-PICK protocol

If a gate's pick does not arrive in-window: record the item **PENDING-SKY-PICK** here (§D) and in
`HANDOFF.md`; the phase stops **complete-except-pick** (all ungated items banked). **No later phase
may build on an unpicked outcome** — it inherits the PENDING status, never assumes a choice. When
Sky picks later (any window), append the pick to §D and re-fire the phase file: its resume rule
reads HANDOFF.md and finishes only the pending item.

## D · Log (append-only; newest last)

| Date | Window/phase | Entry |
|---|---|---|
| 2026-07-16 | Plan authoring (Fable 5) | Ledger seeded. All gates OPEN. SD-1…SD-11 recorded. |
| 2026-07-17 | P1 pre-flight (Opus 4.8) | Handshake CLEAN: HEAD==`9789d1a`, tree clean, no drift in the 4 watched files. Branch `uplift/p1-floor` created off `main`@`9789d1a` (rollback anchor). `design-reviews/` still UNTRACKED (SKY-PREP #1 open) → absolute paths + NO clean ops held train-wide. |
| 2026-07-17 | P1 · Fork E | FYI: no Sky redirect recorded → proceeding **KEEP-DIAMOND** (SD-5). M1 de-collides within the NESW diamond; the grid/list reflow stays fenced. |
| 2026-07-17 | P1 · M2 | **CLOSED** (commit `6d150c4`). L4-F1 (CRIT) verified **NON-REPRO** at per-frame resolution — rAF frame recorder across all advance paths, both RM routes, 0 stale frames; `renderCard`'s clear is already same-paint. Annotated as the SD-9 anchor. No behavioral change. Real-OS-RM = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P1 · Q9 | **CLOSED** (commit `4290195`). 8px HUD stat labels → **10px** floor (largest that holds the single-row HUD; 11px wraps to 2 rows). Size-only, contrast unchanged. Portrait + landscape twins. **Q9 floor = 10px** — P4 badge + P2 `MAC→TERMINAL` relabel must respect (master LAWS 5 & 8). |
| 2026-07-17 | P1 · Q3a | **CLOSED** (commit `71bff76`). 768 SETTINGS un-clip via a new 601–819px tablet-band rule that grows `#cabinet` (container-sizing only; tap targets NOT shrunk, per the report's Q3 a11y note). Verified un-clipped + diamond unchanged + 1440/375 unaffected. **Q3 is now SPLIT: Q3a CLOSED; Q3b (1440 learn-panel) rides with M1 in Item 5.** |
| 2026-07-17 | P1 · M1 gate | **Sky PICKED candidate C** (Balanced) in-session — recorded in §B. A was insufficient; B/C viable; C chosen for reading-at-speed. |
| 2026-07-17 | P1 · M1 | **CLOSED** (commit `b300b41`). Candidate C de-collision (≤600 phantom 42→36 + tokens →clamp(112px,37vw,165px)): +10/+12/+15px clearance @375/390/430 (was −8px overlap). Learn-mode fold relief (arena compaction + panel 38→32vh + learn cabinet scroll): pre-hint + retry1 show all 4 tokens; retry2/reveal scroll. `:focus-visible` intact. Arcade diamond full-size (unchanged). Real-iPhone-Safari-375 touch = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P1 · Q3b | **CLOSED** (commit `b5c3a50`). 1440 learn diamond + bottom bar un-clip via general `body.learning-mode #cabinet{overflow-y:auto}` (reachable by scroll; 1440 arcade hero untouched, verified overflow:hidden). **Residual (honest):** grow-jump not fully stabilized — a strict "never move" fix needs a panel overlay (structural), out of scope for this MED; reachability (L4-F4's core) restored. |
| 2026-07-17 | P1 · STOP | **All 5 CRITICALs CLOSED; Q3 both halves CLOSED same window (C picked in-session).** Green gate clean (56/56, 0 console errors); `git diff 9789d1a..HEAD` = index.html only (+53/−6); PROTECT surfaces intact (1440 hero + 768 diamond + focus-ring + RM). Evidence: `build-plan/reports/P1-verification-evidence.md`. **STOP on `uplift/p1-floor` @ `b5c3a50`** — Sky merges (ff-only, train order P1→P6). |
| 2026-07-17 | P2 pre-flight (Opus 4.8) | Branch `uplift/p2-frontdoor` created off `uplift/p1-floor`@`b5c3a50` (rollback anchor). Base green gate PASS (56/56). No drift (main==origin==`9789d1a`). `design-reviews/` still UNTRACKED → absolute paths + NO `git clean` + stage only named files. BEFORE captures banked (title fresh+seeded @375/768/1440; og-image.png + favicon.svg as-is) → `captures/p2/before/`. |
| 2026-07-17 | P2 · Fork C | **Sky PICKED: KEEP & fix** (plan gate). Q1 builds the one-line NBSP fix; keep-vs-remove comparison mocks still rendered into `captures/p2/forkC/` for Sky's later final call. |
| 2026-07-17 | P2 · Fork D | **Sky PICKED: NO / keep-generic** (plan gate). Title copy unchanged; no PARKING-LOT entry created. |
| 2026-07-17 | P2 · reconcile | **Ledger sync (resume).** A prior P2 window banked Q4/Q1/Q2 and left Q5 in-tree but did NOT record here or in HANDOFF (checkpoint-discipline lapse). Reconciled from git (source of truth); the three closure rows below were added retroactively. Going forward the ledgers are kept current per checkpoint. |
| 2026-07-17 | P2 · Q4 | **CLOSED** (commit `dcd858e`). Invalid `@supports not (A) and not (B)` → De Morgan `not ((background-clip:text) or (-webkit-background-clip:text))`. Old grammar parses to 0 rules (dropped); corrected = valid CSSSupportsRule in live CSSOM. Forced-fallback render = solid `var(--accent)` rgb(61,216,196) on title bg rgb(14,17,22) = **10.64:1** (≫ AA). Zero visual change on supporting browsers. Older-WebKit/non-Chromium = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P2 · Q1 | **CLOSED** (commit `9e5c46d`). Controls-orphan fixed by inverting the NBSP binding — each key–label pair now carries its own internal `&nbsp;`, separators revert to breaking ` · `, so wraps land only at a separator. "? HELP" holds intact @1440 and @768; whitespace-only (reading order + SR output unchanged). Fork C=KEEP; keep-vs-remove comparison mocks in `captures/p2/forkC/` for a later final call. |
| 2026-07-17 | P2 · Q2 | **CLOSED** (commit `8f32c49`). `mac` now reads "TERMINAL" at HUD readout + prompt chip + game-over per-category bars via `CATEGORY_LABELS` (toggle already matched). **LAW 8:** restored the 375 single-row HUD WITHOUT dropping below the Q9 10px floor — tightened the ≤600 HUD column gap 10→8px + cat-badge padding 4px7px→4px5px. rows=1 for every category @375 portrait / 812×375 landscape / 768 / 1440; no overflow; 0 console errors. |
| 2026-07-17 | P2 · Q5 | **CLOSED** (commit `3c9c438`). Preload 2 variable woff2 (Inter v20 / JBMono v24) — cold-cache verified byte-matched, each fetched once (no double-download, Ledger L3), both used. Metrics-matched `@font-face` fallbacks (Inter/Arial, JBMono/Menlo) via canvas-derived size-adjust+overrides: command tokens hold height (dH −3→0 vs naive), HUD digits dW 0. 375 tagline no reflow; diamond stable. Residual: Inter proportional ~5px advance variance on the one-line tagline (cosmetic). NEEDS-SKY-DEVICE: real-Safari cold-cache FOUT. |
| 2026-07-17 | P2 · M4 | **CLOSED** (commit `15838b9`). Title→board double-exposure killed via a two-beat scoped to `#title`'s start-of-run exit (content fade over opaque bg → instant cut; RM branch = instant swap, unchanged). rAF sampler: DE metric 0.25→0; RM 0→0. **SD-6/LAW 3 byte-parity PROVEN:** pause settled sha `5679d11d…` before==after; pause+gameover computed `.screen` transition identical; the shared transition + gameover/pauseScr hide lines untouched in the diff. No new flashing. NEEDS-SKY-DEVICE: real-Safari repaint timing. |
| 2026-07-17 | P2 · Q10 | **CLOSED** (commit `e48e310`). Returning-player branch (`mastered>0`, no new gc.v1 key): MASTERED numeral accented (#3DD8C4) + static "Welcome back — pick up where you left off". FRESH shows neither. `role="group"`/aria-label unchanged; not-color-alone. SKY-WORDING. NEEDS-SKY-DEVICE: Safari+VoiceOver. |
| 2026-07-17 | P2 · M5a | **CLOSED** (commit `350f8bb`). favicon.svg recolored to shipped palette (#06f3ff→#3DD8C4, #0a0118→#0E1116); geometry byte-identical (fills+comments-stripped diff = 0 → mascot chain PROTECT held). Raster fallbacks favicon-32.png / favicon-180.png / favicon.ico (16/32/48 PNG-in-ICO) + SVG/PNG/apple-touch/ICO link tags (Ledger L2, same commit). All four icons fetch 200. NEEDS-SKY-DEVICE: real-Safari tab/bookmark (macOS+iOS). |
| 2026-07-17 | P2 · OG-card gate | **Sky PICKED: A — editorial** (in-window). 3 typography-only candidates (A/B/C) rendered <100KB in the shipped identity; A chosen. |
| 2026-07-17 | P2 · M5b | **CLOSED** (commit `c4b06dc`). Exported candidate A as og-image.png (91,984 B, was 323,598). Rewrote og:image:alt + twitter:image:alt to the actual card + dropped the false "Phantom mascot" claim (L1-F2). Tool-07 harness: card + og:title + og:description agree; no mascot in frame or alt. Copy all existing/approved. NEEDS-SKY-DEVICE (post-deploy, Ledger L1): social re-scrape. |
| 2026-07-17 | P2 · Q2-follow-up | **CLOSED — ⚠ EXCEEDS APPROVED PLAN** (commit `c24f34c`). The copy-result SHARE string emitted the raw key ("MAC deck") while the UI shows "TERMINAL" — routed it through `CATEGORY_LABELS` ("TERMINAL deck" etc.). The plan scoped this OUT; isolated, trivially-revertible commit — Sky may drop it. |
| 2026-07-17 | P2 · STOP | **All 7 slate items CLOSED; OG gate RESOLVED (A).** Green gate clean (56/56, 0 console errors); `git diff b5c3a50..HEAD` = 6 intended files only; PROTECT surfaces intact (byline/pitch/deck-floor absent from diff; favicon geometry byte-identical; pause byte-parity). All 9 P2 commits made by ONE window (the earlier "prior window" reconcile framing was an external ledger-sync artifact; git `main..HEAD` is authoritative). Evidence: `build-plan/reports/P2-verification-evidence.md`. **STOP on `uplift/p2-frontdoor` @ `c24f34c`** — Sky merges (ff-only, train order P1→P2). |
| 2026-07-17 | P2 · adversarial + M4-harden | 2 independent skeptics (M4/motion + Q5/Q2/Q4/PROTECT) → **no correctness bug, no PROTECT regression**; Q5 preload URLs empirically re-confirmed against the live css2 endpoint. One LOW nit actioned: dropped M4's deferred rAF cleanup (backgrounded-tab fragility) → commit `78d605a`, re-verified maxDE 0 / RM 0 / pause sha unchanged. **Final tip = `78d605a`** (supersedes the c24f34c stop row above). |
| 2026-07-17 | P3 pre-flight (Opus 4.8 ultracode, Sky-fired) | **§0 hard gate PASSES:** SD-9 anchor recorded non-blank (`renderCard()` token-reset loop, P1 commit `6d150c4`) — S1 may ship. Branch `uplift/p3-winbeat` created off `uplift/p2-frontdoor`@`78d605a` (rollback anchor); base green gate PASS (56/56). No drift (main==origin==`9789d1a`, unmoved → clean branch, no rebase). `design-reviews/` still UNTRACKED → absolute paths + NO `git clean` + stage only `index.html`. Both P3 gates pre-picked by Sky in plan mode (see §B). |
| 2026-07-17 | P3 · Fork A gate | **Sky PICKED: ALL THREE** (plan gate) — held green+✓ · lingering "+N" score-fly · full-command burn-in. Gates S1 only; no throwaway 4-way prototype capture needed (pick in hand → build the picked form directly). |
| 2026-07-17 | P3 · Q7 gate | **Sky PICKED: HEART + SLASHED-CIRCLE** (plan gate) — SPIRITS gains a teal heart glyph (reads "life"), 50/50 star → gold slashed-circle ⊘ (reads "eliminate"). Glyph + aria only (SD-2). |
| 2026-07-17 | P3 · Q6 | **CLOSED** (commit `974f601`). Teal `:focus-visible` ring on a `.wrong` token neutralized: new `.token.wrong:focus-visible` swaps the three ring layers to `--text-secondary` slate (via `--neutral-halo`), geometry byte-identical (outline 2px/offset 3px, border 1px, halo blur/spread/alpha). `!important` required on border-color + box-shadow to beat `.wrong`'s red `!important`. **Measured (computed sRGB):** ring vs base = 7.76:1, vs the composite `.wrong` fill (#362C36) = 5.46:1 — both ≫ SC 1.4.11 3:1. Red fill + ✗ glyph untouched (outcome triple-encoded). Scoped to `.wrong` only; `.correct:focus-visible` symmetric case → PARKING-LOT. |
| 2026-07-17 | P3 · Q7 | **CLOSED** (commit `be022b6`). SD-2 glyph+aria only. SPIRITS: teal `#lives::before` heart (`\2665`) leading the unchanged cursor-block pips + NEW `#lives` aria-label `"{n} lives remaining"` (had none). 50/50: `.lifeline-pip` star clip-path → slashed-circle (bordered circle `::before` + diagonal slash `::after`), same 16×22 box + `--gold` (layout position unchanged); existing `#lifelines` aria-label kept. Chromium-verified: HUD renders ♥▮▮▮ / ⊘⊘⊘, a11y tree "3 lives remaining" / "3 lifelines remaining", no label text changed, no layout shift, console clean. Safari = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P3 · Q8 | **CLOSED** (commit `36d160e`). Pause overlay → true modal. RESUME `.big-btn` inside `#pauseScreen` (same family as START/RETRY, ≥44pt, focus-ring); copy → "Press ESC or tap RESUME to continue."; `role="dialog"` + `aria-modal="true"` + `aria-labelledby`. `togglePause()` now stashes `document.activeElement` BEFORE `setBoardInert(true)`, moves focus to RESUME, and restores focus (un-inert BEFORE restore) on resume; Esc + RESUME both route through it. Focus-onto-RESUME uses a bounded per-frame poll (`focusResumeWhenReady`) because the shared `.screen` `visibility` fade keeps it unfocusable for ~1 fade. **`.screen` fade untouched → LAW 3 byte-parity holds.** Chromium-verified: board inert (arena/bar/mode-toggle), RESUME is the SOLE reachable focusable (Tab contained), Esc resumes + restores focus to pre-pause element, RESUME-click resumes. VoiceOver/NVDA dialog announce = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P3 · S1 (Fork A = ALL THREE) | **CLOSED** (S1a `0cf8bca` CSS+markup + S1b `161ae66` JS). `.token.captured` no longer dissolves (holds green+✓, verified opacity 1 not 0). "+N" score-fly anchored to the token (`earned` captured before `streak++` → matches announce, verified "+10"/"+11"). Arcade: `#win-strap` burns in `state.current.answer` via textContent (multi-arg `cp a.txt b.txt` intact; opaque `--surface-raised` chip). Learn: CORRECT panel gains `card.answer` at hint scale (`.lp-answer--sm` 18px) — **closes L3-F7** (verified "CORRECT! / git diff / hint"). **LAW 1 / SD-3 (same-paint teardown) VERIFIED at DOM level:** advancing clears `correct`/`captured`/`.score-fly`/strap all in `renderCard()`'s `[M2·L4-F1·SD-9]` paint — zero straggle, L4-F1 stays dead. Timing 750/1200ms untouched; aria-live string unchanged; capture-lunge untouched; RM keyframes end visible (static hold). Real-OS-RM + Safari + VoiceOver = NEEDS-SKY-DEVICE. |
| 2026-07-17 | P3 · STOP | **All 4 slate items CLOSED (Q6/Q7/Q8/S1); both gates PICKED (Fork A=ALL THREE, Q7=HEART+SLASHED-CIRCLE).** Green gate 56/56, 0 console errors; `git diff 78d605a..HEAD` = **index.html only** (+168/−17), 5 commits; no new flashing (new keyframes single-iteration `both`); PROTECT intact (capture-lunge, wrong-flash, `.screen` pause fade, aria-live strings, verdict colours, `:focus-visible` double-ring all untouched/held). Evidence: `build-plan/reports/P3-verification-evidence.md`. **STOP on `uplift/p3-winbeat` @ `161ae66`** — Sky merges (ff-only, train order P1→P2→P3). |
| 2026-07-18 | P4 pre-flight (Opus 4.8 ultracode, Sky-fired) | Branch `uplift/p4-studyreport` created off `uplift/p3-winbeat`@`161ae66` (rollback anchor). Base green gate PASS (56/56, script window 1659..2767). No drift (main==origin==`9789d1a`, unmoved → clean branch, no rebase). `design-reviews/` still UNTRACKED → absolute paths + NO `git clean` + stage only `index.html`. Fork F pre-picked by Sky in plan mode (see §B). |
| 2026-07-18 | P4 · Fork F gate | **Sky PICKED: presentation row** (plan gate). M6 builds the presentation half — a "+N this run" credit chip + hatch sliver beside each lifetime mastery bar, a "You answered N of M correctly this run." summary line, and un-hides the Learn MASTERED bar for SR (L6-F8). The c≥3 mastery RULE is untouched (SD-10 fence). Honest note: no per-category correct tally exists in-code today → M6 adds a **session-only** `runCorrectByCat` display counter (never persisted). |
| 2026-07-29 | P4 · M3 | **CLOSED** (commit `d67e2e5`). Never-clip the game-over review box: sticky top/bottom fade overlays + "↓ more" affordance, gated on real `scrollHeight>clientHeight` overflow via `updateMissedReviewFade()`. Verified @375/768/1440: overflow→fade+cue, scrolled→top fade, bottom→last line fully readable+cues gone, 1-short-miss→zero cues (sh==ch 98/98), box→Tab→drill-missed-btn (no trap). Re-confirmed this session with M6+S3 also present (Item 4 end-state). |
| 2026-07-29 | P4 · M6 | **CLOSED** (commit `8e7b61f`, Fork F = presentation row). This-run credit chip+sliver beside lifetime mastery bars + un-hidden Learn MASTERED progressbar. **Adversarial close-out review found 2 real bugs + 1 cosmetic issue, all fixed**: `#learn-progress-bar` leaked into the a11y tree behind title/pause/game-over (fixed → `b34473e`, folded into `setBoardInert()`); the mastered-count wasn't filtered to the active deck, reachable to an ARIA-invalid `aria-valuenow(23) > aria-valuemax(16)` via an ordinary category switch (fixed → `26a6e46`, filter `mastered` through the same `deck` as `total`); the this-run sliver was clipped/invisible at 100% lifetime mastery (fixed → `15e6669`, clamp `left` to `min(pct, 100-width)`). All three re-verified against their exact repro scenarios. Deviation (honest, unfixed — correct behavior): `aria-valuemax` is dynamic (`activeDeck().length`), not the report's literal 56. |
| 2026-07-29 | P4 · S3 | **CLOSED** (SD-1, commit `63da969`). DRILL MISSED becomes the primary CTA + PLAY AGAIN steps to secondary + new `#drill-badge` ("DRILLING MISSES · cleared of N") during a review drill. **Adversarial close-out review: all 7 claims CONFIRMED-OK, 0 defects found** (empirically stress-tested: 5x back-to-back replay, forced N-of-N drill clear, live aria-live MutationObserver — 3 real changes → 3 announcements, 3 no-op calls → 0). One non-blocking design-tension note surfaced (not a bug, not actioned): keyboard/SR focus still lands on `again-btn`, now the *secondary* button on a miss-run, rather than the promoted DRILL MISSED primary — explicit per the PROTECT'd focus contract, Sky's call whether to revisit. Deviation (honest, unfixed — correct behavior): the badge reads unique-cards-cleared-of-N, not a queue position (`pickCard()` draws with repeats). |
| 2026-07-29 | P4 · Item 4 (Master LAW 5) | **END-STATE RE-VERIFY COMPLETE.** New driver `tools/p4-endstate.mjs` drove M3+M6+S3 simultaneously (worst-case 8 misses, fresh+seeded profiles, ×3 widths): no-clip confirmed (`lastItemFits:true` in all 6 combinations), drill-run chrome confirmed (Q9 10px floor holds, single-row HUD holds, real page-level overflow `false` at all 3 widths — an initial cruder `#cabinet.scrollWidth` reading was investigated and found to be a pre-existing, P4-unrelated `.screen`-bleed artifact, not a clip). Master LAW 3 re-smoke (`tools/p2-m4.mjs p4-endstate`): maxDE 0 both RM paths; computed `.screen` transitions byte-identical to P2's recorded values; `git diff` confirms zero lines of the pause/`.screen` code touched by any P4 commit (the definitive proof — a cross-session PNG hash isn't a valid comparison, noted honestly in the evidence report). |
| 2026-07-29 | P4 · STOP | **All 3 slate items CLOSED (M3/M6/S3); Item 4 end-state re-verify CLOSED; adversarial pass CLOSED (2 real bugs + 1 cosmetic fixed, 1 design note surfaced).** Green gate clean throughout (56/56, 0 console errors). `git diff 161ae66..15e6669` = index.html only (+212/−9), 6 commits. PROTECT surfaces intact (focus contract, re-queue mechanic, seeded stats strip, fade byte-parity). Evidence: `build-plan/reports/P4-verification-evidence.md`. **STOP on `uplift/p4-studyreport` @ `15e6669`** — Sky merges (ff-only, train order P1→P2→P3→P4). |
| 2026-07-29 | P5 pre-flight | Branch `uplift/p5-ghost` created off `uplift/p4-studyreport`@`15e6669` (P4's CURRENT tip, not the stale `63da969` `5.md` names — see HANDOFF.md's note). Base green gate PASS (56/56). Master LAW 2 pre-flight (P2+P4 both banked) confirmed. Fork B **PICKED: C — one-shot materialize** coming into this window → built directly. |
| 2026-07-29 | P5 · D-3 (in-play FORK) | Traced `renderCard()`/`answer()`/`learningRetry()`/`learningReveal()`/`nextCard()` — confirmed NO idle non-card-read window exists in the gameplay loop. The two dormant keyframes (`phantom-float`/`phantom-tail-pulse`) are re-homed to the **game-over** Phantom instead of in-play idle time. S2's in-play clause records as **FORKED**; title + game-over bookends proceed as CLOSED. |
| 2026-07-29 | P5 · B1-B4 | **CLOSED** (commits `13c1122`/`34f8d76`/`87cc42a`/`a2b75f6`). Rewrote the dormant keyframes (amplitude/symmetry/tail transform-origin) → added `.phantom-figure` shared placement CSS (+ mobile variant + load-bearing `position:relative` anchors on `#title h1`/`#gameover h2`) → inserted the title + game-over figure instances (reusing `#phantom`'s inner geometry verbatim — zero redraw) → wired the motion (title one-shot materialize, game-over re-homed float/pulse, both finite/RM-safe). Verified: ONE-ACTOR-MOVES PASS, finite-iteration PASS (only sanctioned `cursor-blink` + a newly-found pre-existing unrelated `#stars`/`twinkle` dead-CSS exception), RM parity PASS both paths, photosensitivity PASS (~0.30 Hz), geometry-PROTECT PASS (no new base geometry rules), M4 re-smoke PASS (maxDE 0, diff-absence proof), replay-twice PASS (no stacking). Master LAW 2: 375/1440 clean; 768 showed an intermittent flake proven (via OLD-vs-OLD self-comparison) to be pre-existing and unrelated to this phase — flagged separately, not blocking. Green gate 56/56 throughout. |
| 2026-07-29 | P5 · STOP | **S2 title+game-over bookends CLOSED; in-play clause FORKED (D-3).** Green gate clean (56/56, 0 console errors). `git diff 15e6669..HEAD` = index.html only (+64/−6), 4 commits. PROTECT surfaces intact. Evidence: `build-plan/reports/P5-verification-evidence.md`. **STOP on `uplift/p5-ghost`** — Sky merges (ff-only, train order P1→P2→P3→P4→P5). |
| 2026-07-29 | P6 pre-flight (Sonnet 5) | Branch `uplift/p6-closeout` created off `uplift/p5-ghost`@`a2b75f6` (P5's tip — P5 stopped complete, so base = its tip per §2.1). Base green gate PASS (56/56). No drift (`main`==`origin/main`==`9789d1a`, unmoved). All 30 conservation-table SHAs re-resolved via `git log -1` before work began — every one present, every subject matching (not trusted from the phase file's transcription, per its own instruction). No mockup gates in this phase (§6 of `6.md`: none). |
| 2026-07-29 | P6 · Item 1 | **Whole-train gates at the tip — PASS.** New `tools/p6-captures.mjs` fills 2 coverage holes (seeded@768, in-app RM@768/375) + a new in-play wrong-answer photosensitivity probe (measured lockout window 1894/1896/1983ms across 3 runs vs the code's 1900ms constant; iteration-count confirmed `'1'` live + by static grep). First rAF-loop probe attempt produced untrustworthy numbers (environment-throttling artifact, documented and replaced with `setInterval`-based polling — see `P6-verification-evidence.md`). `p5-anim-audit.mjs`/`p5-oneactor.mjs` re-run at tip: PASS (with an honest note on a benign cursor-blink 3-vs-6 double-count, investigated not hand-waved). `p4-endstate.mjs`/`p5-law2-domcheck.mjs` deliberately not re-run (overwrite hazard / nothing to A/B); substituted a code-diff proof instead. 3 fade-check legs via `p2-m4.mjs`: intra-session hash MATCH, cross-session transition-string MATCH, code-inferred whole-train proof the `.screen` rule was never touched. Diff audit: 20 files, all accounted for. Banked `f5aece0` (bootstrap) + `74f0e20`. |
| 2026-07-29 | P6 · Item 2 | **Conservation → ACTUAL — 19/19 reconciled (the phase's hard gate).** `00_master.md` §4 rewritten: 18 CLOSED + 1 (S2) CLOSED-with-clause-FORKED, 0 PENDING-SKY-PICK, 0 DEFERRED. Both arithmetic traps (Q3 one-ID/two-commits; S2 one-ID/two-rows) stated explicitly. All 30 SHAs re-verified a second time as this item's own gate. Banked `0bfb775`. |
| 2026-07-29 | P6 · Item 3 | `DEVICE-GATE-CHECKLIST.md` written — 28 deduped items, one consolidated on-device session. Nothing checked off; zero real-device passes exist for this train. Banked `a9a54e7`. |
| 2026-07-29 | P6 · Item 4 | `DEPLOY-CHECKLIST.md` written — master §7 L1-L6 as an ordered list + 5 de-facto riders folded in (not new L-numbers) + the `c24f34c` pre-merge call flagged. Both merge paths stated. CNAME diff verified empty before asserting no DNS changes. Banked `81bddf6`. |
| 2026-07-29 | P6 · Item 5 | Train report written (`reports/2026-07-29_GhostCode_UPLIFT_TrainReport.md`) — per-phase spans re-derived from git log, ACTUAL conservation table, gate results, both checklists linked, full open-items list for Sky. **SD-8 provenance claim independently re-verified via commit trailers** (not copied from the phase file): 23 `Claude Opus 4.8` + 7 `Claude Sonnet 5` = 30, exact match; the 7 Sonnet commits are `b34473e`/`26a6e46`/`15e6669` (M6's adversarial fixes) + all 4 P5 commits. This window's own model (Sonnet 5) tagged honestly against SD-8's Opus directive. Banked `a79c95b`. |
| 2026-07-29 | P6 · STOP | **Train complete.** All 6 phases stopped complete on stacked branches; **nothing merged** — `main`==`origin/main`==`9789d1a`, unmoved through the entire train. Conservation: 19/19 (18 CLOSED + 1 CLOSED-with-clause-FORKED). Green gate clean throughout (56/56, 0 console errors). P6 itself changed zero lines of `index.html`/`cards.js` — confirmed at every checkpoint. Evidence: `build-plan/reports/P6-verification-evidence.md`; synthesis: `build-plan/reports/2026-07-29_GhostCode_UPLIFT_TrainReport.md`. Open for Sky: merge (either path in `DEPLOY-CHECKLIST.md`) → the 28-item `DEVICE-GATE-CHECKLIST.md` → `c24f34c` keep/drop → Fork C final call. **STOP on `uplift/p6-closeout`** — Sky merges (ff-only, train order P1→P2→P3→P4→P5→P6). **This is the last car — there is no P7.** |
