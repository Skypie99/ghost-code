> **📋 PROMPT 5 / 6 — copy this entire file.** Fire in a FRESH window on **Opus 4.8 ultracode, max effort**. Run the prompts strictly in order **1 → 2 → 3 → 4 → 5 → 6**, one phase per window (map + rules in `00_master.md`; the ledgers `DECISIONS.md` + `HANDOFF.md` carry state between windows).

# GHOST CODE UPLIFT — PHASE 5 of 6: THE GHOST IS REAL
**Fire in a FRESH window. Self-contained — do not rely on chat history.**

> **MODEL NOTE:** Authored by **Claude Fable 5 (max effort), 2026-07-16**, in the same session
> that authored the Part-3 report (report dated 2026-07-10). **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** — Sky's standing
> directive for this train (recorded in DECISIONS.md SD-8); Sky fires this window herself, which is
> Sky-initiated Opus under the global model rule. Tag execution provenance honestly in the evidence
> report (this prompt + the report carry the judgment; the executing model carries the hands).

## 0 · What this phase is

The identity signature (S2 — TOP-5 #5; HIGH L8-F1 both halves + L4-F5 + partially L1-F8): put the
Phantom on the title (first frame a reviewer sees), let the in-play Phantom breathe between answers
(wire the dormant idle-float keyframes), and bookend game-over — completing the
favicon→mascot→title identity chain. Scheduled LAST of the build phases BY LAW: the title was
settled by P2 (Q1/Q4/Q10/M4) and game-over by P4 (M3/M6/S3); the Phantom lands layout-neutral on
SETTLED surfaces (master LAW 2). **The whole phase is gated by Fork B, front-loaded.** Budget tag:
**JUDGMENT-HEAVY** (signature motion restraint). Branch: `uplift/p5-ghost` off **P4's tip**.

## 1 · Read-first (mandatory, in this order)

1. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/HANDOFF.md` —
   FIRST: if this phase already has checkpoint entries, you are RESUMING — continue from the last
   banked commit; never redo banked work (this is the resume rule in §3; HANDOFF decides resume vs
   fresh start).
2. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/DECISIONS.md` —
   the train ledger. Standing decisions SD-1…SD-11 bind this phase; §B holds gate states; §D holds
   any pick recorded since authoring (a pick made in an earlier window reaches you ONLY through this
   file).
3. **The design authority:** `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/2026-07-10_GhostCode_Design_Review.md`
   — read at least the executive summary, §PROTECT list, §Sky-Decision taste forks, and the slate
   blocks for S2. Those blocks are quoted VERBATIM in §4 below for self-containedness, but if
   any transcription doubt arises, the report on disk wins.
4. `/Users/skypie/Games/pacman-code-trainer/CLAUDE.md` — the repo's green gate, ff-only merge
   protocol, textContent rule, and gc.v1 persistence rules.
5. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/00_master.md` —
   the train map (where this phase sits, what later phases consume from it).

## 2 · Pre-flight

1. **Base = `uplift/p4-studyreport` tip**, present + green. Recovery protocol if `origin/main` moved: content-containment check — the prior phase's content already on `origin/main` (squash-merged) → REBASE this branch onto `origin/main`; unrelated drift → MERGE main in; either way run the FULL green gate before work item 1.
2. **Master LAW 2 check:** P2 and P4 must both be banked on this branch's history (their evidence
   reports exist). If either stopped complete-except-pick on an item that moves layout
   (M1/Q3b are P1; og-image is asset-only — neither blocks S2; but if P4's M6 row is
   PENDING, note that the game-over layout may still gain a row later: position the Phantom
   so an added row cannot collide — state this in the evidence).
3. Create `uplift/p5-ghost`; record base SHA in HANDOFF.
4. BEFORE captures: title (fresh + seeded) + game-over + in-play idle at 3 widths; RM captures both
   paths; the PRESS START finite blink recording (the finite-pulse pattern S2 mirrors).

## 3 · The rails (non-negotiable)

1. **READ-ONLY fences held verbatim.** Presentation/UX only — game mechanics (movement, Phantom
   travel/aim, difficulty, scoring, the mastery rule) and the command curriculum are DATA, fenced to
   the report's Sky-Decision Notes. The NESW `.n/.e/.s/.w` direction mapping is load-bearing for
   swipe AND arrow-keys — never remap it. All card-derived text via `textContent`, never `innerHTML`.
   `localStorage['gc.v1']` stays additive (never rename keys). Lifelines stay session-only. No
   invented copy/claims/numbers. **Accessibility outranks aesthetics** (WCAG 2.2 AA floor; no new
   flashing; reduced-motion respected on both paths: `prefers-reduced-motion` AND `body.reduce-motion`).
2. **Anchors, not line numbers.** The report's `index.html` line refs are hints pinned to `9789d1a`;
   this file has been edited since. Locate every target by selector/function anchor and re-derive.
3. **Stacked train.** Branch `uplift/p5-ghost` off `uplift/p4-studyreport` tip. One commit per work item — the commit IS
   the checkpoint. An unexplained regression costs ONE reverted commit + a note, never the phase.
4. **Checkpoint & handoff protocol.** BANK BEFORE NEW WORK (never start an item with unbanked
   changes; prefer finishing + banking the current item). Update `HANDOFF.md` at EVERY checkpoint.
   Before any risky multi-file step: checkpoint first, note in HANDOFF what is about to change and
   why, THEN act. A window killed at any moment must leave HANDOFF accurate to the last checkpoint.
   Continuity and recoverability beat speed. **The resume rule:** a fresh window re-fired on this
   file reads HANDOFF.md first and continues from the last banked commit — banked work is never redone.
5. **Mockup-gate mechanics.** A gate stops ONLY the gated item; every ungated item proceeds. If the
   pick does not arrive in-window: record PENDING-SKY-PICK in DECISIONS.md §D + HANDOFF.md and stop
   the phase complete-except-pick. No later phase builds on an unpicked outcome.
6. **PROTECT-ACTIVE.** Re-render every touched PROTECT surface before/after (§7) — the full PROTECT
   list is in the report; if any protected surface changed unintentionally, that is a defect.
7. **Evidence honesty.** Tag everything verified / code-inferred / NEEDS-SKY-DEVICE. All captures
   are Chromium (the audit rig) — NOT Safari/WebKit; Sky's device pass is the only Safari truth.
   Any contrast claim is MEASURED (sampled pixels / computed ratio), never asserted.
8. **Intended files only.** This phase's diff may touch `index.html` (code), the train
   ledgers (`DECISIONS.md`, `HANDOFF.md`), and new files under
   `design-reviews/fable-audit/build-plan/reports/` — no other application source changes.
9. **STOP discipline.** Stop ON the branch. Merge, push, build, deploy, DNS = Sky's hands.
   **ff-only warning (repeat to Sky at stop):** merging this phase's tip fast-forwards `main`
   through ALL prior phases' commits — merge in train order; skipping requires rebasing later
   branches + re-gating. Serial per project: no other phase runs while this one is open.
10. **Design-Compiler note (Const. Art. 2.4):** this phase's before/after capture set is the compile
    input; routing to Dani is Sky's step — do not self-declare UI-DONE.

```bash
# THE GREEN GATE — run after every work item; re-derive the script window EVERY time (lines drift)
cd /Users/skypie/Games/pacman-code-trainer
node --check cards.js
open=$(grep -n '<script>' index.html | head -1 | cut -d: -f1)
close=$(grep -n '</script>' index.html | tail -1 | cut -d: -f1)
sed -n "$((open+1)),$((close-1))p" index.html | node --check -
node test/cards.test.js
# browser smoke: python3 -m http.server 8000 → play one card end-to-end, console must be clean
```

**Capture rig:** reuse `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/tools/`
(read `RUNBOOK.md` first; `PW_PREFIX` lives OUTSIDE the repo; Playwright's bundled ffmpeg has no
filters — use `-r 3`, never `-vf fps=3`). BEFORE captures precede work item 1; AFTER captures precede
the stop. All → `build-plan/reports/captures/p5/{before,after}/`.

## 4 · Work items — verbatim spec + the gate

**Item 1 — MOCKUP GATE: Fork B (title-Phantom form; gates the WHOLE phase).** Build the three
candidates as **code-free mocks** (standalone HTML snapshots or CSS-toggled previews saved under
`build-plan/reports/captures/p5/candidates/` — NO commits to `index.html` before the pick):
(a) idle-floating cursor near the wordmark/CTA · (b) static ghost-glyph mark · (c) brief one-shot
materialize on load. Every candidate shows its reduced-motion rendering (static glyph) beside the
full-motion one, at 1440 + 375, on the SETTLED post-P2 title. Present → **STOP for Sky's pick.**
No pick in-window → the phase stops cleanly, zero code commits, candidates banked; S2 =
PENDING-SKY-PICK.

**Item 2 — S2 build (after the pick; one commit per surface).**
- **Title:** the picked form near the wordmark/CTA — reuse the existing `#phantom` markup/geometry
  (the favicon↔mascot chain is PROTECT: no redraw), `aria-hidden` (decorative), layout-neutral
  (absolute/no reflow), no contrast loss on wordmark/CTA/focus rings (MEASURED).
- **In-play:** wire the dormant idle-float keyframes — idle-only BETWEEN answers, never during a
  card read or feedback window (ONE-ACTOR-MOVES is PROTECT), tiny amplitude, finite iteration
  mirroring the PRESS START finite-pulse pattern (never `infinite`), gated on BOTH RM paths.
- **Game-over:** the bookend presence — layout-neutral on the settled P4 layout; if M6 is PENDING,
  positioned so a later row cannot collide.
- **Master LAW 2 re-verify:** 768/375 captures of title + game-over prove zero reflow (overlay
  diff vs the §2 BEFORE captures — only the Phantom pixels may differ).
- RM: the static ghost-glyph renders on BOTH paths; no new flashing anywhere (photosensitivity
  PROTECT); title→board fade (P2's M4 sequencing) re-smoked with the Phantom present.

┄┄┄ BEGIN VERBATIM SLATE BLOCK S2 (from the report, §The improvement slate) ┄┄┄

### S2 — Phantom Presence at Title and Game-Over (Identity Bookends)

**Where:** Title/attract screen, in-play, and game-over — all widths.

**What (mockup-ready):** Bring a single restrained Phantom presence onto the TITLE screen — the spectral terminal-cursor idle-floats near the wordmark/CTA, rendering as a static ghost-glyph under reduced-motion — so the ownable identity appears on the first frame a reviewer sees. Wire the dormant idle-float keyframes (`phantom-float`, `phantom-tail-pulse`, already defined but unused at index.html:499-511) so the in-play Phantom gently breathes BETWEEN answers only — idle-only, tiny amplitude, reduced-motion-gated, and never during a card read, honoring ONE-ACTOR-MOVES and readability-at-speed. Give the GAME-OVER screen a Phantom presence too, as the last frame a reviewer sees. This completes the favicon → mascot → title identity chain and is the presentation-safe expression of the "spectral terminal haunting" hook. Does not change what the Phantom does — its travel/aim logic stays fenced. Not a neon arcade splash; the calm one-accent restraint holds.

**Why:** Resolves L8-F1 (HIGH/confirmed, 02_findings.md lines 727-732 — both title and game-over halves), L4-F5 (LOW/code-inferred, lines 418-422), and partially L1-F8 (MEDIUM/render-observed, lines 106-110). Grounded in the L8 distinctiveness verdict (lines 673-726): the app has an ownable visual hook that currently surfaces only in-play. Serves FUN by giving the mascot a presence at the moments a player's attention is highest (arrival and departure) and LEARNING by reinforcing a consistent, calming identity around the drills rather than adding noise. Serves the taste anchor by strengthening the signature restraint instead of adding decoration.

**Player/Learner impact + Craft/Cohesion:** The Phantom becomes a through-line — first frame, gameplay heartbeat, last frame — instead of appearing only mid-session, making the identity legible and memorable within seconds (addressing L8-F1) and closing the mascot-geometry gap flagged in L4-F5/L1-F8. Raises craft cohesion to the level of the family's other projects by finishing the favicon-to-mascot-to-title chain rather than leaving it half-built.

**Accessibility note:** Fully reduced-motion-gated per the app's dual reduced-motion architecture — under reduced motion, the title and game-over Phantom render as a static ghost-glyph (no float/pulse animation), and the in-play idle-breathe is suppressed entirely rather than substituted. No information is conveyed by color alone — presence/identity is communicated via shape (the Phantom glyph itself), not hue. Photosensitivity safety is preserved: amplitude stays tiny, motion is idle-only, and it is strictly gated off during any card read, so no flicker or motion competes with active gameplay or reading tasks. This is presentation-only — it does not touch mechanics, scoring, or curriculum, and does not alter the fenced ONE-ACTOR-MOVES travel/aim logic.

**Effort:** S — wires two already-defined, currently-unused CSS keyframes (`phantom-float`, `phantom-tail-pulse` at index.html:499-511) into three existing screens; no new animation authoring, no mechanics changes. No dependencies on or conflicts with other proposals identified.

**Verification:** Render-state check on title, in-play (idle window only), and game-over screens at all widths, both with and without reduced-motion enabled, confirming: (1) Phantom appears within the first frame on title and last frame on game-over, (2) in-play breathing occurs only between answers and never during a card read, (3) reduced-motion renders the static ghost-glyph with animation fully suppressed. Note for the record: no SVG viewer was available when this proposal was grounded, so L8-F1.svg content was inferred from its paired finding text rather than viewed directly — visual confirmation against the actual SVG asset should be part of this verification pass. NEEDS-SKY-DEVICE not required for this pass (no OS-level reduced-motion or Safari-specific dependency beyond standard `prefers-reduced-motion` media query support, which can be verified via preview emulation).

┄┄┄ END VERBATIM SLATE BLOCK S2 ┄┄┄

## 5 · Phase laws & re-verifications

- **Master LAW 2** — layout-neutral on settled surfaces; overlay-diff proof at 768/375.
- **ONE-ACTOR-MOVES** — a motion capture proving the Phantom never animates during a card read.
- **Finite iteration** — computed-style check: no `infinite` on any new animation.
- **RM parity** — both RM paths show the static glyph; the in-play idle is fully suppressed.
- Title→board transition re-smoke (M4's sequencing intact with the new title element).

## 6 · Mockup gates in this phase

- **Fork B** (Item 1) — gates the whole phase; candidates are code-free.

## 7 · PROTECT-ACTIVE recapture set

- **ONE-ACTOR-MOVES / no ambient motion during a card read** — recording.
- **Photosensitivity floor** — no new flash; timings unchanged (measured).
- The **favicon↔mascot geometry chain** — the title/game-over Phantom reuses the existing node
  geometry; overlay before/after.
- The **two-line title pitch + byline** — byte-unchanged.
- The **full RM / forced-colors / aria-live layer** — RM captures both paths.

## 8 · Device checklist (NEEDS-SKY-DEVICE)

- **Real OS-level Reduce Motion:** the title reads right with the static glyph; nothing floats.
- **iPhone look** of the title Phantom at 375 (does the restraint hold on a small bright screen?).
- Real-device feel of the idle-float amplitude (Chromium timing ≠ device compositor).

## 9 · Stop conditions + evidence (all gates before stopping)

- Green gate clean (zero new failures) after the LAST work item.
- Every §5 phase law / re-verification satisfied and evidenced.
- Before/after captures saved under `build-plan/reports/captures/p5/`.
- `git diff` (base..HEAD) touches ONLY this phase's intended files (§3.8).
- Every phase-assigned proposal is CLOSED, FORKED, or PENDING-SKY-PICK — nothing silently dropped.
- Write `build-plan/reports/P5-verification-evidence.md`: per-item before/after pairs,
  measurements, evidence tags (verified / code-inferred / NEEDS-SKY-DEVICE), the phase's device
  checklist (§8) restated for Sky, commit list with one-line rationale each.
- Update `DECISIONS.md` §D (gates presented/picked/pending) and
  `HANDOFF.md` (final checkpoint: STATUS / REMAINING / KNOWN ISSUES / NEXT ACTION + resume command;
  repeat the ff-only merge-order warning).
- **STOP on `uplift/p5-ghost`.** No merge, no push, no deploy — Sky's hands.

## 10 · Conservation entries (this phase)

| ID | Status at stop |
|---|---|
| S2 | → record CLOSED / FORKED / PENDING-SKY-PICK (+ commit ref) |
