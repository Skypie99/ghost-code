> **📋 PROMPT 1 / 6 — copy this entire file.** Fire in a FRESH window on **Opus 4.8 ultracode, max effort**. Run the prompts strictly in order **1 → 2 → 3 → 4 → 5 → 6**, one phase per window (map + rules in `00_master.md`; the ledgers `DECISIONS.md` + `HANDOFF.md` carry state between windows).

# GHOST CODE UPLIFT — PHASE 1 of 6: THE FLOOR
**Fire in a FRESH window. Self-contained — do not rely on chat history.**

> **MODEL NOTE:** Authored by **Claude Fable 5 (max effort), 2026-07-16**, in the same session
> that authored the Part-3 report (report dated 2026-07-10). **EXECUTE on Opus 4.8 ultracode MAX EFFORT, all sub-agents max effort** — Sky's standing
> directive for this train (recorded in DECISIONS.md SD-8); Sky fires this window herself, which is
> Sky-initiated Opus under the global model rule. Tag execution provenance honestly in the evidence
> report (this prompt + the report carry the judgment; the executing model carries the hands).

## 0 · What this phase is

The value-front-loaded first car: it closes **all five CRITICALs** of the Fable audit — the
reduced-motion stale-feedback correctness bug (M2 → CRITICAL L4-F1) and the ≤600px play-field
collapse cluster (M1 → CRITICALs L2-F1/L2-F2/L5-F1/L5-F2 + HIGH L5-F5) — plus the severed-frame
clips (Q3 → HIGH L5-F4 + MED L4-F4) and the 375 HUD legibility floor (Q9 → MED L6-F3).
Items: **M2, Q9, Q3a, [gate], M1 + Q3b** in that internal order. Budget tag: **JUDGMENT-HEAVY**
(M1 is the gnarliest layout work in the train) — fire on a fresh budget. Branch: `uplift/p1-floor`.

## 1 · Read-first (mandatory, in this order)

1. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/HANDOFF.md` —
   FIRST: if this phase already has checkpoint entries, you are RESUMING — continue from the last
   banked commit; never redo banked work (this is the resume rule in §3; HANDOFF decides resume vs
   fresh start).
2. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/DECISIONS.md` —
   the train ledger. Standing decisions SD-1…SD-11 bind this phase; §B holds gate states; §D holds
   any pick recorded since authoring (a pick made in an earlier window reaches you ONLY through this
   file).
3. **The design authority:** `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/2026-07-10_GhostCode_Design_Review.md`
   — read at least the executive summary, §PROTECT list, §Sky-Decision taste forks, and the slate
   blocks for M1, M2, Q3, Q9. Those blocks are quoted VERBATIM in §4 below for self-containedness, but if
   any transcription doubt arises, the report on disk wins.
4. `/Users/skypie/Games/pacman-code-trainer/CLAUDE.md` — the repo's green gate, ff-only merge
   protocol, textContent rule, and gc.v1 persistence rules.
5. `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/build-plan/00_master.md` —
   the train map (where this phase sits, what later phases consume from it).

## 2 · Pre-flight (all must pass before work item 1)

1. **VERSION HANDSHAKE.** `git log -1` — the audited SHA is **`9789d1a`** (main == origin at
   authoring). If HEAD ≠ `9789d1a`: diff `index.html`, `cards.js`, `og-image.png`, `favicon.svg`
   against `9789d1a`. Cosmetic drift (docs, qa-reports) → note it and proceed. **Meaningful drift in
   those four files → STOP**, write the divergence to DECISIONS.md §D + HANDOFF.md, and flag Sky —
   never build against drift.
2. **Untracked-authority check.** If `design-reviews/` is still untracked (SKY-PREP #1 not done):
   proceed, but record it in HANDOFF and **never run any clean operation**.
3. **Fork E FYI (record in DECISIONS §D).** The report fences the 375 grid/list reflow as a Sky+Dani
   input-model decision (it would remap the swipe + arrow-key NESW mapping). This phase proceeds on
   the report's keep-diamond side. If Sky has recorded a redirect in DECISIONS §D, HALT M1 and stop
   the phase after M2/Q9/Q3a.
4. **Base = `main` at the handshake SHA.** Recovery protocol (if origin/main moved): content-
   containment check — if the change is already-shipped train content (squash), rebase; if unrelated
   drift, merge main in; either way run the FULL green gate before item 1.
5. Create `uplift/p1-floor`; record the base SHA as the rollback anchor in HANDOFF.
6. BEFORE captures (rig, §3): gameplay + learn-reveal + learn-pre-hint + token-focus at 375/768/1440,
   plus an RM run capture (both RM paths) — these are the M1/M2 baselines.

## 3 · The rails (non-negotiable)

1. **READ-ONLY fences held verbatim.** Presentation/UX only — game mechanics (movement, Phantom
   travel/aim, difficulty, scoring, the mastery rule) and the command curriculum are DATA, fenced to
   the report's Sky-Decision Notes. The NESW `.n/.e/.s/.w` direction mapping is load-bearing for
   swipe AND arrow-keys — never remap it. All card-derived text via `textContent`, never `innerHTML`.
   `localStorage['gc.v1']` stays additive (never rename keys). Lifelines stay session-only. No
   invented copy/claims/numbers. **Accessibility outranks aesthetics** (WCAG 2.2 AA floor; no new
   flashing; reduced-motion respected on both paths: `prefers-reduced-motion` AND `body.reduce-motion`).
2. **Anchors, not line numbers.** The report's `index.html` line refs are hints pinned to `9789d1a`;
   this file has been edited since. Locate every target by selector/function anchor and re-derive.
3. **Stacked train.** Branch `uplift/p1-floor` off `main` (post-handshake). One commit per work item — the commit IS
   the checkpoint. An unexplained regression costs ONE reverted commit + a note, never the phase.
4. **Checkpoint & handoff protocol.** BANK BEFORE NEW WORK (never start an item with unbanked
   changes; prefer finishing + banking the current item). Update `HANDOFF.md` at EVERY checkpoint.
   Before any risky multi-file step: checkpoint first, note in HANDOFF what is about to change and
   why, THEN act. A window killed at any moment must leave HANDOFF accurate to the last checkpoint.
   Continuity and recoverability beat speed. **The resume rule:** a fresh window re-fired on this
   file reads HANDOFF.md first and continues from the last banked commit — banked work is never redone.
5. **Mockup-gate mechanics.** A gate stops ONLY the gated item; every ungated item proceeds. If the
   pick does not arrive in-window: record PENDING-SKY-PICK in DECISIONS.md §D + HANDOFF.md and stop
   the phase complete-except-pick. No later phase builds on an unpicked outcome.
6. **PROTECT-ACTIVE.** Re-render every touched PROTECT surface before/after (§7) — the full PROTECT
   list is in the report; if any protected surface changed unintentionally, that is a defect.
7. **Evidence honesty.** Tag everything verified / code-inferred / NEEDS-SKY-DEVICE. All captures
   are Chromium (the audit rig) — NOT Safari/WebKit; Sky's device pass is the only Safari truth.
   Any contrast claim is MEASURED (sampled pixels / computed ratio), never asserted.
8. **Intended files only.** This phase's diff may touch `index.html` (code), the train
   ledgers (`DECISIONS.md`, `HANDOFF.md`), and new files under
   `design-reviews/fable-audit/build-plan/reports/` — no other application source changes.
9. **STOP discipline.** Stop ON the branch. Merge, push, build, deploy, DNS = Sky's hands.
   **ff-only warning (repeat to Sky at stop):** merging this phase's tip fast-forwards `main`
   through ALL prior phases' commits — merge in train order; skipping requires rebasing later
   branches + re-gating. Serial per project: no other phase runs while this one is open.
10. **Design-Compiler note (Const. Art. 2.4):** this phase's before/after capture set is the compile
    input; routing to Dani is Sky's step — do not self-declare UI-DONE.
11. **Cascade-revert caveat (this phase only):** M1/Q3b/Q9 edit overlapping CSS regions in the
    `@media (max-width:600px)` block and the token/arena base rules — a single-commit revert there
    may cascade into its neighbors. If a revert conflicts, revert the coordinated set (M1+Q3b+Q9
    commits) and re-land — still never the whole phase.

```bash
# THE GREEN GATE — run after every work item; re-derive the script window EVERY time (lines drift)
cd /Users/skypie/Games/pacman-code-trainer
node --check cards.js
open=$(grep -n '<script>' index.html | head -1 | cut -d: -f1)
close=$(grep -n '</script>' index.html | tail -1 | cut -d: -f1)
sed -n "$((open+1)),$((close-1))p" index.html | node --check -
node test/cards.test.js
# browser smoke: python3 -m http.server 8000 → play one card end-to-end, console must be clean
```

**Capture rig:** reuse `/Users/skypie/Games/pacman-code-trainer/design-reviews/fable-audit/tools/`
(read `RUNBOOK.md` first; `PW_PREFIX` lives OUTSIDE the repo; Playwright's bundled ffmpeg has no
filters — use `-r 3`, never `-vf fps=3`). BEFORE captures precede work item 1; AFTER captures precede
the stop. All → `build-plan/reports/captures/p1/{before,after}/`.

## 4 · Work items — internal order + verbatim specs

**Item 1 — M2 (bank the worst bug first).** Independent, tiny, TOP-5 #1. Locate the wrong/correct
judgment-class application and the next-card render path by anchor (search `.correct` / `.wrong`
class toggles and the render/deal function; report hints: `renderCard` token reset region,
`rec-rm-run` f029 evidence). Clear judgment classes BEFORE (or in the same paint as) the next-card
render on BOTH RM paths. **Then write DECISIONS.md SD-9: the code anchor (function/selector) where
the clear now lives** — P3's S1 terminates its held win-state in this exact clear. Verify with an
f029-style frame-diff: RM run, wrong answer → next card; no frame may show the new prompt wearing
the old card's colors. Commit.

┄┄┄ BEGIN VERBATIM SLATE BLOCK M2 (from the report, §The improvement slate) ┄┄┄

### M2 — Fix stale judgment-class carryover on the RM path (class-reset ordering)

**Where:** Reduced-motion card transition (`@media (prefers-reduced-motion)` and the in-app `body.reduce-motion` route); `index.html` ~1722-1730 (renderCard token reset) and ~1953-2003 / ~1999-2003 (photosensitivity timing windows, unchanged); render frame `assets/motion-frames/rec-rm-run__dark__1440/f029.png` (stale) → `f030.png` (cleared).

**What (mockup-ready):** Clear the `.correct`/`.wrong` judgment classes on all tokens in the same paint/frame as `renderCard`'s prompt-and-token DOM swap — not after it. Today, under reduced motion, a new prompt loads while the previous card's judgment paint is still showing for roughly one 3fps beat (~333ms): render-observed in f029, a fresh prompt displays while a decoy token still wears the green correct-glow and another still wears the red wrong-border. This is a pure class-reset ORDERING fix — presentation-safe, touches no mechanic, no new state, no new timing values, no change to which classes exist or what they mean. Co-design with S1 (S1's held green must terminate in this same clear).

**Why:** Resolves L4-F1 (CRITICAL, `design-reviews/fable-audit/02_findings.md` lines 392-397) and the L4 motion verdict / Hard Gate 2 — Reduced Motion section (lines 358-391). Cross-referenced against `assets/annotated/L4-F1.svg` and render frames `rec-rm-run__dark__1440/f029.png→f030.png`. "A frame of wrong truth is worse than no feedback" — in a speed-read command trainer, a momentary confident-but-wrong judgment can teach the wrong answer, undermining LEARNING; it also breaks the taste anchor of trustworthy, precise feedback that the game's judgment system is built on.

**Player/Learner impact + Craft/Cohesion:** Removes a moment where the game visually lies to the exact learners the reduced-motion accommodation is meant to protect, so the outcome the player sees always matches the outcome that's true. Corrects the accommodation to deliver what it already promises: persistent, accurate state with no motion required. A same-paint reorder, not a re-timing or re-encoding change, so it costs nothing in scope and raises trust in the judgment system's craft.

**Accessibility note:** This bug lives inside the app's own reduced-motion accommodation, which complements WCAG 2.3.3 (Animation from Interactions) and 2.2.2 (Pause/Stop/Hide) via `prefers-reduced-motion`/`body.reduce-motion`. As shipped, that accommodation momentarily misleads the users it exists to protect by painting a confidently-wrong judgment state; the fix restores the accommodation's promise without touching the accessibility floor. Not-color-alone is preserved: outcome remains glyph + hint + pip + announcer, never color-alone (WCAG 1.4.1, unaffected). Photosensitivity is preserved: the Three Flashes / single-iteration flash timing gate (1900ms wrong-window / 750-1200ms correct-window) is untouched (WCAG 2.3.1, still PASS). No change to focus, contrast, or `:focus-visible` behavior. Protect-list guard-rail: do not widen this fix beyond class-clear ordering — leave the 1900ms/750-1200ms windows, the single-iteration flash rule, the capture-lunge overshoot spring, and the aria-live announcer copy exactly as they are.

**Effort:** S — same-paint reordering fix, no new state or mechanic; dependency: co-design with S1 (shared clear point for S1's held green).

**Verification:** Render-state comparison of the RM path frame sequence (`rec-rm-run__dark__1440`) confirming no frame shows a new prompt with a prior card's `.correct`/`.wrong` class still painted (i.e., f029's stale-paint condition no longer reproduces). Re-check WCAG 2.3.1 timing windows are numerically unchanged in code (`index.html` ~1953-2003) and re-confirm outcome encoding is not color-alone. Evidence honesty: render frames are Chromium (Playwright/Chromium 1228) captures, not Safari/WebKit — code-inferred timing mechanics (setTimeout offsets) are code-cited, not independently re-verified on-device here; full cross-browser confirmation is NEEDS-SKY-DEVICE.

┄┄┄ END VERBATIM SLATE BLOCK M2 ┄┄┄

**Item 2 — Q9 (375 HUD label floor).** Independent of the arena geometry; bank it before the gate.
Note for the record (report Q9 is the shortest slate block): contrast is already ~7.8:1 — this is a
SIZE floor only; do not touch color. Commit.

┄┄┄ BEGIN VERBATIM SLATE BLOCK Q9 (from the report, §The improvement slate) ┄┄┄

### Q9 — Enlarge the 8px HUD labels at 375

**Where:** The HUD labels at 375 (SCORE/BEST/STREAK/SPIRITS/50-50/CATEGORY).

**What (mockup-ready):** Raise the minimum HUD label size at 375 to a legible floor — today they shrink to 8px. Contrast is fine at ~7.8:1, but the SIZE undercuts low-vision legibility.

**Why:** Resolves finding L6-F3 (/Users/skypie/Games/pacman-code-trainer/design-reviews/*/02_findings.md lines 566-571), citing index.html:1172, :1181, :1258. No other findings invented.

**Player/Learner impact + Craft/Cohesion:** Pairs with M1 (the 375 re-architecture); makes the HUD legible for low-vision players at the smallest breakpoint without altering the HUD's information content.

**Accessibility note:** A size-floor fix (legibility) — contrast and motion are unaffected, and no color-alone signal is introduced or removed. Fence respected: mechanics/curriculum untouched, no NESW remap.

**Effort:** S — dependency: pairs with M1 (the 375 re-architecture); no conflicts with other proposals.

**Verification:** Render state comparison against assets/base/gameplay__dark__375.png and assets/annotated/L6-F3.svg to confirm the label floor size renders correctly at 375; measurement of rendered label size post-fix.

┄┄┄ END VERBATIM SLATE BLOCK Q9 ┄┄┄

**Item 3 — Q3a (the 768 SETTINGS un-clip half of Q3).** The cabinet-overflow half is independent of
the learn-panel machinery; the 1440 learn-panel half (Q3b) is coordinated with M1 in Item 5 — do NOT
do it here. Commit as `Q3a`.

┄┄┄ BEGIN VERBATIM SLATE BLOCK Q3 (from the report, §The improvement slate) ┄┄┄

### Q3 — Un-clip severed controls (768 SETTINGS + 1440 learn-panel)

**Where:** #cabinet / #bar control bar at 768px (primary play screen, SETTINGS button) and the learn panel at 1440px (grow-jump clipping the bottom category/control bar). Both are inside the fixed-height `#cabinet` frame (`height:min(780px,94vh); overflow:hidden`, index.html:250/268).

**What (mockup-ready):** At 768, when `#bar` wraps to a second row (desktop bar rules apply above the 600px breakpoint — index.html:643 flex-wrap:wrap, with the 600px override at index.html:1211 not reaching this width), the cabinet grows to fit its own content instead of holding a hard px/vh cap: either the cabinet's height constraint becomes a `min-height` with `overflow:visible` above a defined "bar has wrapped" state, or the second `#bar` row is given reserved space inside the existing cap so SETTINGS lands fully inside the rounded frame — no button half-hangs off the card edge, no visible bite out of the chrome. At 1440, the learn panel (currently content-sized, snapping ~50px per index.html:1737-1740's hideLearningPanel/showLearningPanel phases) gets a reserved max-height sized to its longest state (reveal+explain) so the diamond and bottom bar never move when the panel content changes — the bottom category/control bar stays fully visible at every learn-state (pre-hint → retry → reveal → correct) instead of having only button-tops peeking above the fold. Both fixes are the same category of bug: a fixed/capped container clipping a control that legitimately grew — solve by either reserving room for the worst-case content height or letting the frame's height respond to it, never by letting overflow:hidden silently eat a button.

**Why:** Resolves L5-F4 (HIGH, CONFIRMED 2x skeptic — `assets/base/gameplay__dark__768.png`; #bar wraps to a 2nd row, SETTINGS orphaned and visibly severed by the cabinet's rounded bottom edge at a common tablet/split-screen width) and L4-F4 (MEDIUM, render-observed — `assets/motion-frames/rec-learn-moment__dark__1440/f018.png, f030.png`; panel-grow pushes the diamond down far enough that the bottom bar is clipped, tops of buttons only, at pre-hint size). Serves LEARNING: a severed SETTINGS button or a clipped control bar reads as broken software to a cold reviewer mid-lesson, which breaks trust in the curriculum before the player even reaches the next card. Serves the taste anchor directly — material depth "used with restraint" means the frame chrome never bites into its own controls; a clipped button is the opposite of "always legible."

**Player/Learner impact + Craft/Cohesion:** A player on an iPad-width viewport (768) can now always reach SETTINGS without guessing whether the half-visible sliver is clickable. A player moving through a learn-mode sequence at desktop width (1440) keeps the full control bar in view across every state, so the reflow reads as intentional polish rather than a bug. This is a floor-level craft fix, not a flourish — it removes the single most "is this broken" signal on the primary play screen and turns the cabinet frame into something that behaves as a deliberate container at every state, reinforcing the boutique/editorial signature (frame is engineered, not improvised).

**Accessibility note:** No color-dependent signal involved — this is a structural/layout fix, not a status indicator. No motion is added; if the 1440 fix uses a height transition instead of pure reserved-space, it must respect `prefers-reduced-motion` (no animated height change when reduced motion is set — snap directly to final state, matching current reduced-motion handling elsewhere in the frame). Preserves existing keyboard/focus order for SETTINGS and the bottom bar — no DOM reordering, only container sizing. Preserves the fixed dark theme and current touch-target sizing; button tap targets must not shrink to fit.

**Effort:** S-M. No dependency on other proposals; does not touch the NESW mapping, scoring, difficulty, or curriculum content (pure container CSS: cabinet height/overflow rule + learn-panel reserved height). Conflicts to check: any other proposal touching `#cabinet` sizing or the learn-panel transition (index.html:1737-1740) should land in the same pass to avoid two competing height rules.

**Verification:** Re-render `gameplay__dark__768.png` equivalent (or live Chromium screenshot at 768px) and confirm SETTINGS sits fully inside the cabinet's rounded edge with no clipped pixels. Re-render the 1440 learn-mode sequence (pre-hint → retry → reveal → correct) and confirm the bottom control bar stays fully visible with zero vertical shift of the diamond across states. Both checks can be done via the existing Chromium render harness; flag the standard "Chromium render is not Safari/WebKit" caveat since Safari's flex-wrap and overflow clipping can render fractionally differently — NEEDS-SKY-DEVICE spot-check on an actual iPad-width Safari window for the 768 case specifically, since that is the real-world device class this bug targets.

┄┄┄ END VERBATIM SLATE BLOCK Q3 ┄┄┄

**Item 4 — MOCKUP GATE: M1 de-collision candidates (gates M1 + Q3b only).** Build 2–3 candidate
treatments WITHIN the directional diamond (per SD-5; e.g. smaller Phantom · capped E/W token height ·
reserved center gutter — mixes allowed). Render each as a CSS-toggled variant or standalone mock and
capture at 375 (arcade board, learn-pre-hint, learn-reveal) **using the longest command strings in
the deck** (derive: longest `card.answer` + longest prompt from `cards.js`). Each candidate capture
must SHOW: Phantom-to-token clearance, the reveal token unoccluded, all four tokens + the S token
on-screen with the learn panel open. Present the board → **STOP the gated items for Sky's pick**
(record in DECISIONS §B/§D). No pick in-window → M1 + Q3b = PENDING-SKY-PICK; stop the phase
complete-except-pick (M2/Q9/Q3a banked).

**Item 5 — M1 + Q3b (one coordinated pass, after the pick).** Build the picked candidate. M1's
within-diamond constraint is the invariant (SD-5): the `.n/.e/.s/.w` anchors keep their directional
meaning. Q3b (the 1440 learn-panel grow-jump + bottom-bar clip) lands INSIDE this pass — both touch
the `#learn-panel` / cabinet-height machinery, and M1's fold criterion wins where they overlap.
Acceptance (all render-verified at the real widths): (a) no Phantom/token overlap in ANY state incl.
glow-halo/motion-wake; (b) learn-reveal correct token fully readable at 375; (c) all four tokens
on-screen with the learn panel open at 375 (the S token fold criterion); (d) 1440 learn-panel opens
without shifting/clipping the bottom bar; (e) `:focus-visible` double-ring intact on tokens.
One commit for M1, one for Q3b (cascade-revert caveat noted in §3.11).

┄┄┄ BEGIN VERBATIM SLATE BLOCK M1 (from the report, §The improvement slate) ┄┄┄

### M1 — Restore readability-at-speed at ≤600px (within the directional diamond)

**Where:** `#arena` token diamond (`.token`, `.n/.e/.s/.w`), `#phantom` glow footprint, `#learn-panel` max-height — all scoped to the `@media (max-width:600px)` breakpoint in `index.html`.

**What (mockup-ready):** At ≤600px (measured at 375) the compass diamond collides: the Phantom (42px, center-anchored) is sandwiched against the E/W tokens (`clamp(124px,41vw,200px)` ≈ 154px each) with sub-pixel clearance, its glow/motion-wake crossing into the command text (L2-F1, L5-F1); the same collision lands directly on the correct token at the learn-mode reveal, occluding the exact string being taught (L2-F2, L5-F2); the growing `#learn-panel` (`max-height:38vh`) pushes the south token to or below the fold so only a card's fourth option ever half-appears (L5-F5); and longer command strings wrapping to two lines further eat the already-thin clearance, compounding the collision on exactly the content that's hardest to read (L5-F6). Fix: re-tune the `clamp()` sizing on `.token` and `#phantom`, add breathing-room spacing/z-index layering between the Phantom's glow footprint and token text, and raise/reflow `#learn-panel`'s footprint so the south token stays fully on-screen at ≤600px. This is a sizing/spacing/z-index change only — the `.n{top:0}/.e{right:0}/.s{bottom:0}/.w{left:0}` anchor assignments that encode the NESW direction mapping are untouched, so the swipe/arrow-key mapping (fenced, load-bearing for both input methods) does not move.

**Why:** The play field is where the game's actual mechanic — reading a command token correctly under time pressure — happens. Right now the frame (title, HUD, modals) reflows gracefully at 375 while the field it wraps around collapses, so a reviewer who scrolls past the polished hero screenshot hits a 375 capture that reads as "great taste, incomplete responsive QA" (per the audit's own unity note). Closing this gap doesn't add anything to the game — it lets the existing diamond breathe at the width most players (and every recruiter on a phone) will actually use.

**Player/Learner impact + Craft/Cohesion:** In Arcade mode, players stop losing spirits to a mascot that's visually eating the token they're trying to read, not a wrong answer they chose. In Learn mode, the reveal state — the single moment designed to burn the correct command into memory — stops hiding the string it exists to teach, and the fourth answer option stops disappearing below the fold where a newcomer could believe there are only three choices.

**Accessibility note:** WCAG 2.2 AA anchor is SC 2.5.8 (Target Size Minimum) plus general legibility-at-speed — de-colliding the diamond increases effective spacing and reduces visual crowding around each token, which is a net accessibility gain, not a trade. No contrast values change, no motion is added or removed (the existing `prefers-reduced-motion` handling and the capture-lunge overshoot spring are untouched), no color-alone outcome signaling is introduced, and photosensitivity constraints (single-iteration flashes ≥~1900ms apart, soft-glow not full-fill) are not touched by this proposal. Evidence honesty: the underlying findings (L2-F1 CONFIRMED, L2-F2 CONFIRMED, L5-F1 PLAUSIBLE, L5-F2 CONFIRMED, L5-F5 CONFIRMED, L5-F6 code-inferred/mixed) are Chromium/Playwright renders — not Safari/WebKit — so iOS Safari's dynamic-toolbar `vh` behavior and `overflow-wrap` shaping should be re-verified on a real device before calling this closed.

**Protected (explicitly untouched by this proposal):** the NESW swipe/arrow-key direction mapping (anchor positions unchanged); the `renderExplain` reveal-panel content and its "THE ANSWER IS" typography; the capture-lunge overshoot spring; ONE-ACTOR-MOVES during a card read; hand-authored mnemonics and decoys (data, not touched); the difficulty/verdict color grammar and the full RM/forced-colors/aria-live layer; photosensitivity flash timing/soft-glow rule; correct touch input (dominant-axis swipe, 28px threshold, pinch-zoom); `:focus-visible` rings and focus trap/restore; the 1440 hero diamond and the 768/375 responsive citizens outside the collision zone this proposal targets; the refusal of the synthwave/CRT aesthetic (grid stays hidden); the favicon↔mascot geometry chain (no redraw); `[EXPLAIN-TEXTCONTENT]`, `[PERSIST-ADDITIVE] gc.v1`, `[LIFELINE-SESSION-ONLY]`.

**Effort:** Small–Medium. CSS-only change scoped to the existing `@media (max-width:600px)` block (`index.html` ~lines 1156-1211) plus the `.token`/`#phantom` base rules it overrides (~lines 521-558) and the `#learn-panel` cap (~lines 1186-1194). No JS logic, no `cards.js` changes, no new DOM elements.

**Verification:** Green gate (`node --check cards.js`, inline-`<script>` check, `node test/cards.test.js`) plus a targeted visual re-capture at 375/390/430/600px in both Arcade and Learn modes (token-focus, wrong-flash, learn-reveal, learn-attempt1 states) confirming: (1) no Phantom/token overlap in any state, (2) south token fully visible on-screen during learn-panel reveal, (3) two-line-wrapped commands (e.g. `git show-branch`) still clear the Phantom. Flag as Chromium-render-only pending Sky's real-device (Safari/WebKit) pass per the audit's standing caveat. Route through Dani's Design Compiler gate before marking UI DONE (Const. Art. 2.4) since this touches on-screen layout.

┄┄┄ END VERBATIM SLATE BLOCK M1 ┄┄┄

## 5 · Phase laws & re-verifications

- **LAW (master #1):** M2 lands here and its clear anchor is recorded in SD-9 — P3 depends on it.
- **LAW (master #6):** anchors not line numbers, every item.
- **Fold criteria become the train's reference:** record in the evidence report the exact 375 fold
  check (S token visible with panel open, longest content) and the 1440 zero-shift check — P3
  re-runs both with S1's content added (master LAW 4).
- Q9's floor value (the final px size) → evidence report — P4's badge must respect it (master LAW 5).
- M2 verification is a frame-diff, not an eyeball: capture the RM wrong→next transition at 3fps+
  and diff consecutive frames for stale paint.

## 6 · Mockup gates in this phase

One gate: **M1 de-collision form** (Item 4; = Fork E's UI half). Gates M1 + Q3b only; M2/Q9/Q3a are
never blocked by it.

## 7 · PROTECT-ACTIVE recapture set (before/after pairs for Sky's eye)

- The **1440 hero diamond** and the **healthy 768 diamond** (strongest responsive citizens — a 375
  fix must not regress them).
- The **capture lunge** (overshoot spring) — one motion capture proving it untouched.
- Token **`:focus-visible` double-ring** at all three widths.
- The **RM/forced-colors layer** — RM captures on both paths, post-M2.

## 8 · Device checklist (NEEDS-SKY-DEVICE)

- **M1: real-iPhone Safari 375 touch play** (serve `python3 -m http.server 8000` on the LAN) —
  readability-at-speed with real fingers is the core-mechanic floor; Chromium cannot prove it.
- Real OS-level Reduce Motion spot-check of the M2 fix (CDP emulation ≠ the OS setting).
- Quick VoiceOver pass over the arena (should be unchanged — confirm no regression).
- **Q3a: real iPad-width Safari spot-check of the 768 `#bar`/SETTINGS un-clip** — flex-wrap +
  overflow clipping render differently in Safari than Chromium, and 768 iPad-class windows are
  the real-world device class this finding targets (per the Q3 block's own Verification).

## 9 · Stop conditions + evidence (all gates before stopping)

- Green gate clean (zero new failures) after the LAST work item.
- Every §5 phase law / re-verification satisfied and evidenced.
- Before/after captures saved under `build-plan/reports/captures/p1/`.
- `git diff` (base..HEAD) touches ONLY this phase's intended files (§3.8).
- Every phase-assigned proposal is CLOSED, FORKED, or PENDING-SKY-PICK — nothing silently dropped.
- Write `build-plan/reports/P1-verification-evidence.md`: per-item before/after pairs,
  measurements, evidence tags (verified / code-inferred / NEEDS-SKY-DEVICE), the phase's device
  checklist (§8) restated for Sky, commit list with one-line rationale each.
- Update `DECISIONS.md` §D (gates presented/picked/pending; write SD-9) and
  `HANDOFF.md` (final checkpoint: STATUS / REMAINING / KNOWN ISSUES / NEXT ACTION + resume command;
  repeat the ff-only merge-order warning).
- **STOP on `uplift/p1-floor`.** No merge, no push, no deploy — Sky's hands.

## 10 · Conservation entries (this phase)

| ID | Status at stop |
|---|---|
| M1 | → record CLOSED / FORKED / PENDING-SKY-PICK (+ commit ref) |
| M2 | → record CLOSED / FORKED / PENDING-SKY-PICK (+ commit ref) |
| Q3 | → record CLOSED / FORKED / PENDING-SKY-PICK (+ commit ref) |
| Q9 | → record CLOSED / FORKED / PENDING-SKY-PICK (+ commit ref) |

*Q3 may close SPLIT: if the M1 gate isn't picked in-window, record the single cell as
"Q3a CLOSED (commit <sha>) / Q3b PENDING-SKY-PICK" — a bare CLOSED is only correct when BOTH
halves landed. P6's conservation-to-ACTUAL sweep carries the split notation forward as-is.*
